"use strict";
/*
const gulp         = require('gulp'),
	  $            = require('gulp-load-plugins')();

module.exports = (options) => {
   return () => {
		return gulp.src(options.src)
				.pipe($.word())
				.pipe(gulp.dest(options.dest));
	};
};

lazyRequireTask('word', './tasks/', {
	 src: word ,
	dest:
});

*/